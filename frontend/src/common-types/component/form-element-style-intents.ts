/**
 * Copyright © [Daigaku].
 *
 * @author tmsnvk
 */

/**
 * Enum values matching the intent style options of input form elements.
 */
export enum CoreInputElementStyleIntent {
  LIGHT = 'light',
}

/**
 * Enum values matching the intent style options of select form elements.
 */
export enum CoreSelectElementStyleIntent {
  LIGHT = 'light',
}

/**
 * Enum values matching the intent style options of disabled input form elements.
 */
export enum CoreDisabledInputElementStyleIntent {
  DEFAULT = 'default',
}

/**
 * Enum values matching the intent style options of submit input form elements.
 */
export enum CoreSubmitInputElementStyleIntent {
  DARK = 'dark',
}

/**
 * Enum values matching the intent style options of textarea form elements.
 */
export enum CoreTextareaElementStyleIntent {
  LIGHT = 'light',
}
